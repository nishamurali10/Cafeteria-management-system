<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="fixed z-50 min-w-full px-4 transform -translate-x-1/2 top-6 left-1/2 md:min-w-0" data-controller="flash" data-turbo-cache="false">
      <div class="max-w-full md:whitespace-nowrap flex rounded-full py-4 px-6 items-center <%= %w[alert error].include?(name.to_s) ? 'bg-pink-100 text-pink-800' : 'bg-blue-100 text-blue-800' %>">
        <%= content_tag :p, msg, :id => "flash_#{name}", class: "flex-grow" %>
        <span class="flex-shrink-0 ml-5 cursor-pointer" data-action="click->flash#dismiss">
          <span class="text-2xl font-light leading-none">&times;</span>
        </span>
      </div>
    </div>
  <% end %>
<% end %>